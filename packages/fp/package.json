{
  "name": "@guru/fp",
  "version": "0.0.0",
  "description": "funciona",
  "license": "private",
  "private": "true",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib",
    "!*.map"
  ],
  "directories": {
    "test": "test"
  },
  "scripts": {
    "commit": "cz",
    "build": "rimraf lib &&  tsc --build && pnpm run lint --fix",
    "lint": "eslint src/**/*.ts test/**/*.ts",
    "watch": "tsc --watch",
    "test": "pnpm run build && jest --logHeapUsage",
    "test:cov": "jest --coverage",
    "test:watch": "jest --watch",
    "typedoc": "typedoc"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "dependencies": {
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@knodes/typedoc-plugin-monorepo-readmes": "^0.22.5",
    "@types/jest": "^27.5.0",
    "@types/lodash": "^4.14.182",
    "@types/node": "^17.0.31",
    "@types/rimraf": "^3.0.2",
    "typedoc": "^0.22.15",
    "rimraf": "^3.0.2",
    "syncpack": "^7.2.1",
    "typescript": "^4.6.4",
    "typesync": "^0.9.1"
  },
  "exports": {
    ".": "./lib/index.js"
  }
}